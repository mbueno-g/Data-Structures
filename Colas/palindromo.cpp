
// PALINDROMO = sucesión de caracterres tal que al recorrerla de izquierda
// a derecha (ignorando blancos intermedios) es la misma que si se recorre de
// derecha a izquierda.

// DOS PILAS


template <typename T>
bool palindromo(pila<T> p)
{
    pila<T> *aux = pila_vacia<T>(); 
    
}